# Ultra-Fast Transcription & Diarization Requirements
# Optimized for sub-0.2x processing time ratio

# Core ML libraries with GPU acceleration
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.35.0
accelerate>=0.24.0

# Audio processing libraries
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
numpy>=1.24.0

# Speaker diarization
pyannote.audio>=3.1.0

# Performance monitoring and optimization
psutil>=5.9.0
sentencepiece>=0.1.99

# Optional GPU optimizations (install based on your system)
# For NVIDIA GPUs:
# torch[cuda]
# For Apple Silicon:
# torch[mps] (included in torch>=2.0)

# Development and debugging (optional)
tqdm>=4.66.0

# Audio format support
ffmpeg-python>=0.2.0

# Note: Make sure you have FFmpeg installed on your system:
# macOS: brew install ffmpeg
# Ubuntu/Debian: sudo apt install ffmpeg
# Windows: Download from https://ffmpeg.org/

# Hugging Face authentication (if needed)
huggingface-hub>=0.17.0 