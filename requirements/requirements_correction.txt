# Transcription Correction Requirements
# Advanced post-processing and accuracy improvements

# Core Lightning MLX (already in requirements_lightning_mlx.txt)
lightning-whisper-mlx>=0.0.10

# Natural Language Processing
spacy>=3.6.0
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0  # For GPU acceleration

# Enhanced NER models for phrase extraction
datasets>=2.10.0

# Grammar and text correction
language-tool-python>=2.7.0
nltk>=3.8.0

# Text processing utilities
regex>=2023.0.0
textdistance>=4.6.0
python-Levenshtein>=0.20.0

# AI models for correction
sentence-transformers>=2.2.0

# Optional: Enhanced spell checking
pyspellchecker>=0.7.0
autocorrect>=2.6.0

# Audio and transcription utilities (if not already installed)
soundfile>=0.13.0
librosa>=0.10.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Async processing
aiofiles>=23.0.0

# Logging and monitoring
tqdm>=4.65.0

# Document processing
python-docx>=0.8.11

# Local LLM support
ollama>=0.1.0
mlx-lm>=0.12.0

# Note: After installation, run:
# python -m spacy download en_core_web_sm
# python -m nltk.downloader punkt wordnet averaged_perceptron_tagger

# For local LLM setup:
# 1. Install Ollama: https://ollama.ai/
# 2. Pull a model: ollama pull llama3.2
# 3. Or use MLX-LM for Apple Silicon 